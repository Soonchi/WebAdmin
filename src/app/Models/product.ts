import {FileHandle} from "./file-handle";

export class Product {
 name!: string;
  categoryName!: string;
  ageRange!: string;
  file!: File;
  image!: string;
  price!: string;
  description!: string;
  quantity!: number;
}
